% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segmented_reg.R
\name{segmented_reg}
\alias{segmented_reg}
\alias{print.edgy_segmented_reg}
\title{Segmented Regression using Linear Splines}
\usage{
segmented_reg(
  formula,
  data,
  events = NULL,
  opts = knot_opts(),
  metric = "bic3",
  conf_level = 0.95,
  progress = rlang::is_interactive(),
  knots,
  ...
)

\method{print}{edgy_segmented_reg}(x, ...)
}
\arguments{
\item{formula}{Model formula (e.g., \code{year ~ rate}).}

\item{data}{Data frame or \code{\link[tibble:tibble]{tibble::tibble}} with variables in \code{formula}.}

\item{events}{Vector with the number of events (e.g., deaths) that correspond
to the rate data in the \code{formula.} If provided, weighted least squares is
used instead of ordinary least squares for fitting regression models.}

\item{opts}{A \code{knots_opt()} object providing knot location options.}

\item{metric}{Metric to use for model selection. One of \code{"bic"}, \code{"bic3"},
\code{"aic"}, or \verb{"aicc".} Default is \code{"bic3"}.}

\item{conf_level}{The confidence level to use for the confidence interval.
Must be strictly greater than 0 and less than 1.
Defaults to 0.95 which corresponds to a 95 percent confidence interval.}

\item{progress}{Whether to show progress bars. By default, progress bars are
enabled in interactive sessions (i.e., if \code{rlang::is_interactive()}
returns \code{TRUE}).}

\item{knots}{A numeric vector of knot locations or a \code{list} of numeric
vectors specifying sets of knot locations. If provided, this overrides the
automatic knot selection performed using \code{knot_opts()} and \code{data}.}

\item{...}{Not currently used.}

\item{x}{A \code{segmented_reg()} object.}
}
\value{
A named-list of class \code{"edgy_segmented_reg"}.
}
\description{
\code{segmented_reg()} fits a linear spline model for the outcome (e.g., rates)
against a time variables (e.g., years). Grid search is performed to find
the best model (i.e., optimal knot locations) according to the \code{metric}
criterion. Information about the best model can be extracted from the
returned object using the \verb{extract_best_*()} set of functions.
}
\details{
\code{\link[lspline:lspline]{lspline::lspline()}} is used to compute the basis for a piecewise linear
spline to estimate coefficients in the segmented regression model.

If \code{\link[mirai:daemons]{mirai::daemons()}} has been used to set persistent background processes,
this function will fit segmented regression models in parallel using all
available processes.
}
\examples{
df <- read.delim(system.file("example.txt", package = "edgy"), header = FALSE)
res <- segmented_reg(V3 ~ V2, data = df)

}
